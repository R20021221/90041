# Handoff Memory

Project: `D:\STUDY\90041\WarehouseManagementPLUS`

User goal: implement COMP90041 A2 WarehouseManagementPLUS from teacher A1 Java base, but work must be incremental and tracked. Do not jump into coding without updating/using `PROGRESS.md`.

Created files:

- `WORKFLOW_FOR_ME_CN_EN.md`: human-facing CN/EN workflow from `requirement.docx`.
- `WORKFLOW_FOR_AI_EN_COMPACT.md`: compact English workflow for AI handoff.
- `PROGRESS.md`: checklist tracker; mark items only after implementation + verification.
- `MEMORY.md`: this handoff note.
- `RULER.md`: immutable project rules for future agents; also marked read-only on disk.

Read/confirmed:

- `requirement.docx` exists and was parsed.
- `src/` has 13 Java files: A1-style scaffold.
- Current base uses 3 args, random 2D map, single menu, operation history.
- A2 requires 5 args, file-loaded multi-floor map, employee roles, shift summaries, payroll/payslips.
- No `.java` files have been modified yet.

Important constraints:

- Future agents must read `RULER.md` first and treat it as immutable.
- Record all modifications, keep `MEMORY.md` updated after conversations, and mark `PROGRESS.md` after completed tasks.
- Before code edits, briefly explain planned direction and logic to the user.
- If unsure, stop, ask the user, and reread relevant `.md` files.
- `requirement.docx` is read-only official source; use it only if Markdown lacks details and user agrees.
- User requested `RULER.md` be English-only; title was cleaned and the file was marked read-only again.
- Keep `WarehouseManagerEngine.java` in default package.
- Need at least 3 packages for other classes.
- No `java.nio.Files`.
- No `System.exit()`.
- Stop using `WarehouseGenerator.java` in A2 flow.
- A2 does not need operation history; use per-employee `ShiftSummary`.
- Use simple collections only (`ArrayList`, `HashMap` OK; avoid `TreeMap`, `TreeSet`, `Vector`).
- Output formatting must closely match requirement examples.
- UML must be manual, not auto-generated by IntelliJ.

Salary note:

- Salary constants table was not extractable from DOCX; it appears as `PreviewHTMLCSSJavaScript`.
- Workflows infer constants from examples: delivery `10.00`, wall hit `0.25`, restricted hit `0.50`, reportee `5.00`.

Current git/status context:

- New docs are untracked.
- Existing untracked items also seen: `Assignment 1 Solution.zip`, workflow/progress docs.
- User removed `Assignment 1 Solution/`; current file listing confirms the folder is gone while the zip remains.
- Do not delete or revert user files.
- User reported `requirement.docx` missing. It was originally at `D:\STUDY\90041\WarehouseManagementPLUS\requirement.docx`; current searches found no copy in project, `D:\STUDY`, Git history, D recycle bin, or same-size/date locations. Windows Recent links still point to the original path.
- User explained original requirement came from a long Ed webpage copied manually. Suggested better source capture: print/save Ed page to PDF, save complete webpage/HTML, or use one-shot select-all copy into txt/md after expanding all content.

Latest conversation update:

- 2026-05-26: Before formal implementation, user asked AI to read `RULER.md` and `WORKFLOW_FOR_AI_EN_COMPACT.md`; both were read. `MEMORY.md` and `PROGRESS.md` were also read to follow the required tracking rules.
- 2026-05-26: User confirmed `requirement.docx` is missing but workflows remain the working source, then asked AI to restate its understanding of the project and the immutable `RULER.md` rules.
- 2026-05-26: User formally started implementation planning and asked how to begin from the workflow. AI rechecked early workflow/progress sections and identified Phase 1 as the first implementation target.
- 2026-05-26: User asked AI to read all Java files under `src/`. AI read the 13 current A1 scaffold files and confirmed they remain default-package, 3-arg, random 2D warehouse, single-menu, operation-history based code.
- 2026-05-26: User decided to defer deleting/deprecating unused A1 files and asked to proceed by implementation phases one by one.
- 2026-05-26: User asked for a detailed description of Phase 1 before starting implementation.
- 2026-05-26: User asked for practical hints/examples on creating Java packages.
- 2026-05-26: User requested that each phase be presented as only one or two steps, with explicit warnings when a step affects later phases.
- 2026-05-26: User asked whether adding `package warehouse` to `Item.java` was correct. AI checked current files: `Item.java` has been moved to `src/warehouses/Item.java`, but it currently has no package declaration; folder name and intended package name should be aligned before compiling.
- 2026-05-26: User fixed `Item.java` path to `src/warehouse/Item.java` and added `package warehouse;`. AI noted the package/folder names now align, but the class Javadoc should move below the package declaration.
- 2026-05-26: User authorized small code edits with explanations. AI completed the first Phase 1 slice: fixed `warehouse.Item` Javadoc position, added imports for default-package A1 classes that still reference `Item`, added `employee`, `warehouse`, `payroll`, `io`, and `IOExceptions` skeleton types, and verified compilation with `javac -d /tmp/wmp-classes ...`.
- 2026-05-26: User moved more warehouse files into packages and asked conceptually how a packaged class can use a class still in the default package, using `WarehouseMap` and `WarehouseGenerator` as the example. AI inspected current files only; no Java edits were made.
- 2026-05-26: User corrected the collaboration pace: future code work should be smaller, explained before execution, and focused on learning. User approved removing same-package imports and deleting unused A2 files. AI removed redundant `import warehouse.Item;` lines from package-local warehouse classes and deleted `src/WarehouseGenerator.java`; `WarehouseMap` still has stale `WarehouseGenerator` references to clean in a later map rewrite.
- 2026-05-26: User asked to clean unused operation-history Java files. AI deleted `OperationHistory.java`, `OperationRecord.java`, and `OperationType.java`, removed `addHistory`/`printHistory` usage, removed the old view-history menu case/text/constants, and confirmed no operation-history references remain. Compile still fails because packaged warehouse classes cannot access default-package `Constants/Messages`, and `WarehouseMap` still references deleted `WarehouseGenerator`.
- 2026-05-26: User asked where the refactor currently stands. AI checked `PROGRESS.md`, `git status`, and current Java file layout before summarizing status.
- 2026-05-26: User suggested moving `Constants.java` and `Messages.java` into the `warehouse` package. AI moved both files, added `package warehouse;`, imported them in default-package `WarehouseManagerEngine.java`, and verified compile now fails only due to stale `WarehouseGenerator` references in `WarehouseMap`.
- 2026-05-26: User requested flexible CSV interfaces with no hardcoded warehouse/employees filenames, but asked not to implement yet. AI confirmed feasibility and added a `PROGRESS.md` design note: warehouse/employees paths come from args; only `data/payslips.csv` is fixed by workflow.
- 2026-05-27: User approved the planned minimal `WarehouseMap` cleanup. AI removed the stale `WarehouseGenerator` field/constructor call and deleted old random-fill helper methods, leaving `WarehouseMap` to initialise only boundary walls, aisles, and start cell for now. Verified no generator references remain and full `javac -d /tmp/wmp-classes ...` compile passes.
- 2026-05-27: User asked for the next work items after Phase 1 and allowed marking progress. AI confirmed Phase 1 is complete in `PROGRESS.md`; next target is Phase 2 command-line validation.
- 2026-05-27: User asked AI to recheck `src` after importing new files, but not inspect the PNG. AI listed paths only and observed new CSV fixtures under `src/data/` plus `src/UML_Scaffold.png`.
- 2026-05-27: User clarified that hardcoded CSV paths are acceptable in early/manual test commands, but actual implementation must remain flexible and accept warehouse/employees paths from input. AI added this as a `PROGRESS.md` design note.
- 2026-05-27: User said they modified `WarehouseManagerEngine.java` and renamed the exceptions package. AI inspected only: exception files now live under `src/IOExceptions/` with `package IOExceptions;`; engine has new A2 placeholder methods (`exitProgram`, `validateArgs`, `loadFiles`, no-arg `runMainMenuLoop`) while old A1 validation/menu methods remain.
- 2026-05-27: User asked to finish Phase 2 before further engine redesign. AI implemented A2 command-line validation in `WarehouseManagerEngine`: exactly 5 args, integer floors/rows/cols, floor range 1..3, rows/cols >=4, warehouse/employees file readability via `java.io.File`, graceful `return` exits, updated `Messages`/`Constants`, compiled successfully, and verified invalid/valid startup command cases.
- 2026-05-27: User approved Phase 3 first slice and requested file/line references. AI converted `WarehouseMap` from 2D to 3D (`floors x rows x cols`), added one forklift per floor, initialized every floor with boundary walls/interior aisles/start cell, removed Warehouse ID display, updated engine to construct `WarehouseMap(floors, rows, cols)` and use floor 1 forklift temporarily, then verified compile and smoke tests. Because CSV is not loaded yet, starting shift currently completes immediately on empty maps.
- 2026-05-27: User approved continuing Phase 3 and asked for changed locations after edits. AI added floor-selection flow: all floors display, prompt with `X` return, invalid floor redisplay, selected-floor movement entry; fixed initial mistake where floor menu used `Q` by adding `MENU_RETURN = "X"`. Compile and smoke tests pass.
- 2026-05-27: User asked to encapsulate the floor header print in `displayFloor` into `Messages` and noted they could not find new constants/messages. AI explained `Constants`/`Messages` now live under `src/warehouse/`, added `Messages.printFloorHeader(int)`, updated `WarehouseMap.displayFloor`, and verified compile.
- 2026-05-27: User asked to begin Phase 4 and split warehouse CSV reading into three steps, with the first step focused on ensuring input works. AI proposed planning first and not editing Java yet.
- 2026-05-27: User approved Phase 4 step 1. AI added `io.WarehouseCsvReader`, wired engine to create `WarehouseMap` and load warehouse CSV from args, skipped header/empty lines, counted rows with 6 fields, printed a temporary `Warehouse CSV rows loaded: n` confirmation, compiled, and verified with `src/data/master_warehouse_1.csv`.
- 2026-05-27: User confirmed validation must print errors and approved continuing. AI extended warehouse CSV loading to parse and validate fields, print exact warehouse-line errors, populate `WarehouseMap` shelves/restricted cells/items, add `ShelfType` support to `Shelf`/`WarehouseCell`, remove the temporary loaded-row count output, compile successfully, and verify against provided fixtures plus a `/tmp` validation fixture for restricted shelf type and shelf type mismatch.
- 2026-05-27: User asked to step back and explain the Phase 4 approach from the beginning, including possible architecture impact and Java knowledge points, before further work.
- 2026-05-27: User approved Phase 5 Step 1. AI added `io.EmployeeCsvReader`, wired engine to read employees CSV from args, validate field count/details/designation, create correct `Employee` subclasses in file order, add reportee storage to `Supervisor`, build direct reportees without validating manager existence/loops, compile successfully, and verify against `employees_1/2/3.csv` plus a `/tmp` field-count fixture.
- 2026-05-27: User asked to review Phase 6 before implementation. AI reviewed payslip workflow rules, current placeholder `payroll.Payslip`, absence of `src/data/payslips.csv`, and planned the implementation without editing Java.
- 2026-05-27: User approved Phase 6 steps 1-2 only. AI implemented `payroll.Payslip`, added `io.PayslipCsvReader`, wired engine to print/process fixed `data/payslips.csv`, allowed missing file without error, validated field count, empty ID/name, and salary fields, compiled successfully, and verified with missing-file and `/tmp` payslip validation fixtures. Writing/generation is deferred.
- 2026-05-27: User approved Phase 6 step 3. AI added `io.PayslipCsvWriter`, termination handling in engine, writes header using `PrintWriter.flush()` when `data/payslips.csv` is missing, preserves existing loaded payslip files untouched when no payslips were generated this session, and verified both cases from `/tmp`. Generated payslip writing in employees order remains deferred until payroll generation exists.
- 2026-05-27: User asked for a current progress report. AI reviewed `PROGRESS.md` and git status; noted phases 1-5 complete, most of phase 6 complete except generated payslip writing, and observed `.class` files in `src` that should be cleaned before final submission.
- 2026-05-27: User approved Phase 7 implementation with attention to ordering/spacing. AI added employee login loop, invalid ID handling, login `X` termination, role-specific operator/supervisor/payroll menus, logout back to login, invalid menu input handling, and smoke-tested invalid ID, each role menu, logout, termination, and invalid menu input.
- 2026-05-27: User approved the first two Phase 8 steps. AI split warehouse shift flow into start/resume/floor-selection methods, added the resume-before-start guard, preserved shared warehouse resume state across employees, handled `Q` as pause back to floor selection, added completion detection/message behavior, compiled successfully, and smoke-tested those paths.
- 2026-05-27: User approved Phase 8 regression checking. AI found no Java fixes needed, verified movement directions, wall/restricted hits, invalid movement input, shelf view/pick/invalid input, delivery at START, pause behavior, and floor-menu return, then marked Phase 8 complete.
- 2026-05-27: User approved Phase 9 implementation. AI connected `ShiftSummary` to `WarehouseMap.startShift/resumeShift`, increments delivered count only on successful delivery at START, wall count only for wall/boundary movement attempts, restricted count only for restricted-area attempts, implemented own-summary menu output, compiled, and verified zero/action/cross-employee counting scenarios.
- 2026-05-28: User approved Phase 10 implementation. AI added salary constants, generated payslips from current summaries for all employees in employees-file order, included payroll manager and no-movement employees, implemented own payslip and payroll-manager all-payslips views, handled missing/header-only/old-vs-new scenarios, verified temporary write-back to `data/payslips.csv`, and marked Phase 10 complete.
- 2026-05-28: User approved Phase 11 implementation. AI added shared employee-summary printing, implemented Supervisor option 5 for direct reportees only, implemented Payroll Manager option 1 for all employees in employees-file order, compiled, and verified privacy/order/counts.
- 2026-05-28: User asked to begin Phase 12 with the first two steps only. AI checked `src/IOExceptions`, confirmed the five required custom exception classes already exist with package `IOExceptions`, compiled successfully, and marked the add-class checklist items complete.
- 2026-05-28: User approved the next small Phase 12 slice. AI refactored warehouse, employee, and payslip CSV readers so invalid row detection throws custom exceptions and the reader loop catches them, prints the same messages as before, and skips the line. Compile and fixture smoke tests passed.
- 2026-05-28: User asked to finish Phase 12. AI connected runtime not-found cases in login/payslip lookup to `NotFoundException`, broadened file-open catches to `IOException`, kept fatal startup behavior unchanged, compiled successfully, and verified invalid login, missing payslip record, missing payslip file, and missing warehouse file scenarios.
- 2026-05-28: User asked for hard compliance tests and then Phase 14 full testing if no blocker. AI verified compile, no `System.exit`, no `java.nio.Files`, package/default-package structure, no complex collections, and Javadoc generation. Phase 14 broad functional tests passed. Remaining cleanup: stale `.class` files in `src`, four over-120-character lines, and exact console comparison against original requirement examples is still limited because the requirement file is missing.
- 2026-05-28: User approved deleting `.class` files. AI removed all `src/**/*.class` compilation outputs and verified no `.class` files remain under `src`.

Next step:

1. Clean Phase 13 submission hygiene issues after user approval.
2. Shorten four long source lines and continue Javadoc/comment polish.
3. Compare exact output wording if requirement examples are recovered; otherwise keep workflow-based output checks documented.
